version: 2

models:
  - name: model_b
    columns:
      - name: date
        tests:
          - unique
  - name: model_a
    columns:
      - name: group_name
        tests:
          - accepted_values:
              values: ['morning']